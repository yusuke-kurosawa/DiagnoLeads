# Lead Analysis & Recommended Actions

**Feature ID**: AI-LEAD-ANALYSIS-001
**Status**: Implemented
**Priority**: Critical (Sales Enablement)
**Last Updated**: 2025-11-23

---

## 📋 Overview

DiagnoLeadsの業界別リード分析・推奨アクションシステム。AI が診断回答から業界特化のインサイトを抽出し、営業チームに即座に実行可能なネクストアクションを提示します。

### ビジネス価値

- **商談化率の向上**: スコア別の最適なアプローチで成約率アップ
- **営業効率化**: AIが自動でリード優先度を判定
- **リソース最適化**: スコアに応じた営業リソースの適切な配分
- **顧客体験の向上**: 業界固有のニーズに即した提案

---

## 🎯 主要機能

### 1. 業界別リード分析テンプレート

5つの業界に特化したリード分析テンプレート：

| 業界キー | 業界名 | 対象 |
|---------|--------|------|
| **it_saas** | IT/SaaS | SaaS企業、ITソリューション |
| **consulting** | コンサルティング | 経営コンサル、専門サービス |
| **manufacturing** | 製造業 | 製造工場、生産ライン |
| **ecommerce** | EC/小売 | Eコマース、オンライン小売 |
| **marketing** | マーケティング | マーケティングエージェンシー |
| **general** | 一般 | デフォルト（業界不明時） |

### 2. テンプレート構成要素

各テンプレートは5要素で構成：

| 要素 | 説明 | 用途 |
|-----|------|------|
| **key_signals** | 重要なシグナル（6項目） | AIが回答から抽出すべきポイント |
| **qualification_criteria** | 高品質リード基準（5項目） | ホットリード判定の指針 |
| **recommended_actions** | 推奨アクション（4段階） | スコア別のネクストステップ |
| **talking_points_themes** | 営業トークテーマ（5項目） | 商談時の会話の軸 |

### 3. スコア別推奨アクション（4段階）

リードスコアに基づく自動アクション提示：

| スコア範囲 | ランク | アクション |
|-----------|--------|-----------|
| **80-100** | 🔥 Hot Lead | 即座に商談設定、デモ・提案書準備 |
| **60-79** | 🟡 Warm Lead | 詳細資料送付、1週間以内フォローアップ |
| **40-59** | 🟢 Cold Lead | ナーチャリングシーケンス追加 |
| **0-39** | ⚪ Not Qualified | 情報提供のみ、定期チェックイン |

---

## 🏢 業界別テンプレート詳細

### 1. IT/SaaS (`it_saas`)

**重要なシグナル**:
1. 現在のシステムの課題や不満
2. 技術スタックとツールの成熟度
3. 予算規模と承認プロセス
4. 導入タイムライン
5. セキュリティ・コンプライアンス要件
6. チームサイズと技術力

**高品質リード基準**:
- ✅ 明確な課題を認識している
- ✅ 予算と決裁権限がある
- ✅ 3ヶ月以内の導入を検討
- ✅ 競合製品と比較検討中
- ✅ 技術的な理解度が高い

**推奨アクション**:
| スコア | アクション |
|-------|-----------|
| 80-100 | 即座にデモを提案。意思決定者との商談を設定。ROI試算を準備。 |
| 60-79 | 詳細な資料を送付。ユースケースを共有。1週間以内にフォローアップ。 |
| 40-59 | ナーチャリングメールシーケンスに追加。成功事例を定期的に共有。 |
| 0-39 | 情報提供のみ。四半期ごとのチェックイン。 |

**営業トークテーマ**:
- システム統合のシンプルさ
- スケーラビリティとパフォーマンス
- セキュリティ認証とコンプライアンス
- TCO削減とROI
- 導入事例と成功ストーリー

---

### 2. コンサルティング (`consulting`)

**重要なシグナル**:
1. プロジェクト管理の課題
2. クライアント獲得コスト
3. 品質管理とナレッジ共有
4. チームのスキルギャップ
5. 成長目標と制約

**高品質リード基準**:
- ✅ 明確な業務課題がある
- ✅ 年間10プロジェクト以上
- ✅ 品質向上に投資意欲あり
- ✅ チームが10名以上
- ✅ デジタル化に前向き

**推奨アクション**:
| スコア | アクション |
|-------|-----------|
| 80-100 | 具体的なワークショップを提案。ROI試算を提示。 |
| 60-79 | ベストプラクティスガイドを提供。ウェビナーに招待。 |
| 40-59 | 事例研究を定期配信。業界動向レポートを共有。 |
| 0-39 | ニュースレター登録を提案。 |

**営業トークテーマ**:
- プロジェクト効率化
- 品質標準化
- ナレッジマネジメント
- チーム協業
- クライアント満足度向上

---

### 3. 製造業 (`manufacturing`)

**重要なシグナル**:
1. 生産効率と稼働率
2. 品質不良率
3. 設備保全の課題
4. 在庫管理の問題
5. デジタル化の進捗

**高品質リード基準**:
- ✅ 生産ライン数が3以上
- ✅ 年間売上10億円以上
- ✅ IoT/DX投資予算あり
- ✅ 品質改善に課題感
- ✅ 経営層の理解あり

**推奨アクション**:
| スコア | アクション |
|-------|-----------|
| 80-100 | 工場視察を提案。PoC（概念実証）を提案。 |
| 60-79 | デモンストレーション実施。投資回収シミュレーション提供。 |
| 40-59 | 成功事例の詳細を共有。業界セミナーに招待。 |
| 0-39 | 情報収集フェーズ。定期的なタッチポイント維持。 |

**営業トークテーマ**:
- 生産性向上
- 品質安定化
- 予知保全
- 在庫最適化
- スマートファクトリー

---

### 4. EC/小売 (`ecommerce`)

**重要なシグナル**:
1. 月間売上規模
2. カート放棄率
3. 顧客獲得コスト
4. リピート率
5. マルチチャネル対応

**高品質リード基準**:
- ✅ 月商1000万円以上
- ✅ CVR改善に課題感
- ✅ データ活用に前向き
- ✅ 成長フェーズにある
- ✅ マーケティング予算あり

**推奨アクション**:
| スコア | アクション |
|-------|-----------|
| 80-100 | 無料トライアルを提案。CVR改善施策を提示。 |
| 60-79 | A/Bテスト事例を共有。コンサルティング提案。 |
| 40-59 | EC最適化ガイドを提供。定期的なヒント配信。 |
| 0-39 | 業界トレンドレポート配信。 |

**営業トークテーマ**:
- CVR向上
- LTV最大化
- パーソナライゼーション
- オムニチャネル
- データドリブン経営

---

### 5. マーケティング (`marketing`)

**重要なシグナル**:
1. マーケティング予算規模
2. ROI測定の課題
3. ツールスタックの成熟度
4. リード獲得数とCV率
5. データ統合の課題

**高品質リード基準**:
- ✅ 月間予算100万円以上
- ✅ ROI測定に課題感
- ✅ MA導入検討中
- ✅ チーム5名以上
- ✅ データ活用意欲あり

**推奨アクション**:
| スコア | アクション |
|-------|-----------|
| 80-100 | マーケティング監査を提案。ROI改善プランを提示。 |
| 60-79 | ツール比較資料を提供。デモアカウント発行。 |
| 40-59 | ベストプラクティスガイド配信。ウェビナー招待。 |
| 0-39 | マーケティングトレンドレポート配信。 |

**営業トークテーマ**:
- ROI測定と最適化
- マーケティングオートメーション
- データ統合と可視化
- リード育成
- アトリビューション分析

---

### 6. 一般 (`general`) - デフォルト

**重要なシグナル**:
1. 現在の課題認識
2. 予算と承認プロセス
3. 導入タイムライン
4. 意思決定者の関与
5. 競合比較状況

**高品質リード基準**:
- ✅ 明確な課題がある
- ✅ 予算がある
- ✅ 6ヶ月以内の導入
- ✅ 意思決定権限あり
- ✅ 情報収集が進んでいる

**推奨アクション**:
| スコア | アクション |
|-------|-----------|
| 80-100 | 即座に商談を設定。提案書を準備。 |
| 60-79 | 詳細資料を送付。1週間以内にフォローアップ。 |
| 40-59 | ナーチャリングシーケンスに追加。 |
| 0-39 | 定期的な情報提供のみ。 |

**営業トークテーマ**:
- 課題解決の具体的アプローチ
- 投資対効果
- 導入の容易さ
- サポート体制
- 成功事例

---

## 🔧 API仕様

### get_lead_analysis_template(industry: str) -> LeadAnalysisTemplate

業界別リード分析テンプレートを取得

```python
from app.services.ai import get_lead_analysis_template

# IT/SaaS業界のリード分析テンプレート
template = get_lead_analysis_template("it_saas")

print(template.key_signals)
# ["現在のシステムの課題や不満", "技術スタックとツールの成熟度", ...]

print(template.qualification_criteria)
# ["明確な課題を認識している", "予算と決裁権限がある", ...]

print(template.recommended_actions["80-100"])
# "即座にデモを提案。意思決定者との商談を設定。ROI試算を準備。"
```

**フォールバック動作**: 未知の業界は`general`テンプレートを返却

---

### get_recommended_action(score: int, industry: str) -> str

リードスコアと業界から推奨アクションを取得

```python
from app.services.ai import get_recommended_action

# スコア85、IT/SaaS業界の推奨アクション
action = get_recommended_action(score=85, industry="it_saas")
print(action)
# "即座にデモを提案。意思決定者との商談を設定。ROI試算を準備。"

# スコア55、コンサルティング業界
action = get_recommended_action(score=55, industry="consulting")
print(action)
# "事例研究を定期配信。業界動向レポートを共有。"
```

**スコア範囲の判定**:
```python
if score >= 80:
    return recommended_actions["80-100"]
elif score >= 60:
    return recommended_actions["60-79"]
elif score >= 40:
    return recommended_actions["40-59"]
else:
    return recommended_actions["0-39"]
```

---

## 📊 AIサービスへの統合

### リード分析時の利用

```python
# AIService.analyze_lead_insights() より抜粋
async def analyze_lead_insights(
    self,
    assessment_responses: Dict[str, Any],
    assessment_title: str,
    industry: str,
    ...
):
    # リード分析テンプレート取得
    lead_template = get_lead_analysis_template(industry)

    # テンプレートデータを構造化
    template_data = LeadAnalysisTemplateData(
        industry=lead_template.industry,
        key_signals=lead_template.key_signals,
        qualification_criteria=lead_template.qualification_criteria,
        talking_points_themes=lead_template.talking_points_themes,
    )

    # AIプロンプト生成（業界コンテキスト含む）
    prompt = PromptTemplates.build_lead_analysis_prompt(
        assessment_responses=assessment_responses,
        assessment_title=assessment_title,
        lead_template=template_data,
    )

    # Claude API 呼び出し
    message = await retry_with_backoff(self._call_claude_api, ...)
    insights_data = self.json_extractor.extract(message.content[0].text)

    # スコアに基づく推奨アクションを追加
    score = insights_data.get("overall_score", 0)
    insights_data["recommended_action"] = get_recommended_action(score, industry)

    # 優先度レベルを自動計算
    hot_lead = insights_data.get("hot_lead", False)
    insights_data["priority_level"] = self._calculate_priority_level(score, hot_lead)

    # フォローアップタイミングを自動計算
    insights_data["follow_up_timing"] = self._calculate_follow_up_timing(score, priority_level)

    return insights_data
```

### プロンプトテンプレート例

```python
prompt = f"""
あなたはB2Bリード分析の専門家です。

<industry_context>
業界: {lead_template.industry}

重要なシグナル:
{'\n'.join(f"- {s}" for s in lead_template.key_signals)}

高品質リード基準:
{'\n'.join(f"- {c}" for c in lead_template.qualification_criteria)}

営業トークテーマ:
{'\n'.join(f"- {t}" for t in lead_template.talking_points_themes)}
</industry_context>

<assessment_responses>
診断名: {assessment_title}
回答データ: {assessment_responses}
</assessment_responses>

<task>
上記の回答から以下を抽出してJSON形式で出力:
1. overall_score (0-100)
2. hot_lead (true/false)
3. pain_points (課題リスト)
4. talking_points (営業トークポイント)
5. insights (インサイト)
</task>
"""
```

---

## 📈 営業フローへの統合

### 1. リアルタイム通知

Hot Lead（スコア80+）検出時に即座に営業チームへ通知：

```python
# 未実装（将来機能）
if insights_data["priority_level"] == "critical":
    # Slack通知
    await slack_service.send_hot_lead_alert(
        lead_id=lead.id,
        score=score,
        recommended_action=insights_data["recommended_action"],
        talking_points=insights_data["talking_points"],
    )

    # メール通知
    await email_service.send_to_sales_team(
        subject=f"🔥 Hot Lead Alert: {lead.name}",
        body=f"Score: {score}\nAction: {recommended_action}",
    )
```

### 2. CRM連携

Salesforce/HubSpotへ自動的にリード情報と推奨アクションを同期：

```python
# 未実装（将来機能）
await salesforce_integration.create_lead({
    "name": lead.name,
    "email": lead.email,
    "score": score,
    "priority": priority_level,
    "recommended_action": recommended_action,
    "talking_points": talking_points,
    "next_follow_up": follow_up_timing,
})
```

### 3. 自動タスク生成

推奨アクションを基に営業タスクを自動作成：

```python
# 未実装（将来機能）
if score >= 80:
    # デモ予約タスク作成
    task = Task(
        assignee=assigned_sales_rep,
        title=f"デモ提案: {lead.name}",
        description=recommended_action,
        due_date=datetime.now() + timedelta(hours=24),
        priority="high",
    )
    db.add(task)
```

---

## 📊 効果測定

### リード分析による商談化率の向上

| 指標 | 分析前 | 分析後 | 改善率 |
|-----|--------|--------|--------|
| **Hot Lead 商談化率** | 18% | 47% | +161% |
| **Warm Lead フォローアップ率** | 34% | 68% | +100% |
| **平均商談設定時間** | 5.2日 | 1.8日 | -65% |
| **営業リソース効率** | - | - | +42% |

*(想定値)*

---

## 🚀 将来の改善

### 1. パーソナライズド推奨アクション

営業担当者のスキル・経験に応じたアクション提示：

```python
class PersonalizedActionEngine:
    def get_action(self, lead_score: int, sales_rep: User, industry: str):
        """営業担当者に最適化されたアクション"""
        base_action = get_recommended_action(lead_score, industry)

        # 新人営業にはより詳細なステップを提示
        if sales_rep.experience_level == "junior":
            return self._add_detailed_steps(base_action)

        # ベテラン営業には柔軟性を持たせる
        return base_action
```

### 2. A/Bテストによるアクション最適化

推奨アクションの効果を測定し、継続的に改善：

```python
# アクションバリアント
RECOMMENDED_ACTIONS_V2 = {
    "it_saas": {
        "80-100": {
            "variant_a": "即座にデモを提案。ROI試算を準備。",
            "variant_b": "無料トライアルを提案。導入事例を共有。",
        }
    }
}

# 自動的にバリアントを選択し効果測定
action, variant = get_action_with_ab_test(score, industry)
```

### 3. 機械学習による動的スコアリング

過去の商談結果から最適なスコア閾値を学習：

```python
class MLLeadScorer:
    def train(self, historical_leads):
        """過去データから最適な閾値を学習"""
        # スコア80-100 → 商談化率を最大化する閾値を発見

    def predict_conversion_probability(self, lead_score, industry):
        """商談化確率を予測"""
        return self.model.predict([[lead_score, industry_encoded]])
```

### 4. 多段階ナーチャリングシーケンス

スコア別の自動メールシーケンス設定：

```python
NURTURING_SEQUENCES = {
    "40-59": [
        {"day": 0, "template": "成功事例紹介"},
        {"day": 7, "template": "業界トレンドレポート"},
        {"day": 14, "template": "ウェビナー招待"},
        {"day": 30, "template": "再度スコアリング"},
    ]
}
```

### 5. リアルタイムスコア更新

リードの行動（メール開封、資料DL等）でスコアを動的に更新：

```python
class LeadScoringEngine:
    def update_score(self, lead_id, event):
        """行動イベントに基づいてスコア更新"""
        score_delta = {
            "email_opened": +5,
            "document_downloaded": +15,
            "demo_requested": +30,
            "pricing_page_viewed": +20,
        }[event]

        lead.score += score_delta
        # スコアが閾値を超えたら推奨アクションを再計算
```

---

## 📂 実装ファイル

| ファイル | 説明 |
|---------|------|
| `/backend/app/services/ai/lead_analysis_templates.py` | リード分析テンプレート定義（247行） |
| `/backend/app/services/ai_service.py` | AIServiceでの統合（analyze_lead_insights） |
| `/backend/app/services/ai/prompt_templates.py` | リード分析プロンプトテンプレート |

---

## 🔗 関連仕様

- [Industry Templates](./industry-templates.md) - 業界別診断テンプレート
- [AI Support](../features/ai-support.md) - AI診断生成機能
- [Lead Management](../features/lead-management.md) - リード管理機能

---

**実装ステータス**: ✅ 完全実装済み（5業界対応）
**拡張機能**: ⏳ CRM連携、自動タスク生成、ML最適化は未実装

# Factory Droid - DiagnoLeads開発ガイド

DiagnoLeadsプロジェクトでFactory Droid（AI駆動開発）を使用するためのガイドです。

## 📚 目次

- [概要](#概要)
- [セットアップ](#セットアップ)
- [利用可能なDroid](#利用可能なdroid)
- [基本的な使い方](#基本的な使い方)
- [開発ワークフロー](#開発ワークフロー)
- [ベストプラクティス](#ベストプラクティス)
- [トラブルシューティング](#トラブルシューティング)

## 概要

Factory Droidは、DiagnoLeadsの開発を効率化するAI駆動開発環境です。以下の機能を提供します：

- **自動化されたコード品質チェック**: リント、型チェック、テストの自動実行
- **マルチテナントセキュリティガード**: テナント分離違反を自動検出
- **OpenSpec統合**: 仕様駆動開発のサポート
- **コードテンプレート**: 一貫性のあるコード生成

### プロジェクト構成

```
DiagnoLeads/
├── .factory/                      # Factory Droid設定
│   ├── config.yml                 # メイン設定ファイル
│   ├── context.md                 # プロジェクトコンテキスト
│   ├── README.md                  # このファイル
│   ├── droids/                    # Droid定義
│   │   ├── dev-workflow.yml       # 開発ワークフロー自動化
│   │   ├── multi-tenant-guard.yml # マルチテナント検証
│   │   └── openspec-sync.yml      # OpenSpec統合
│   └── templates/                 # コードテンプレート
│       ├── api_endpoint.py
│       ├── model.py
│       ├── service.py
│       └── component.tsx
│
├── backend/                       # FastAPIバックエンド
├── frontend/                      # Reactフロントエンド
├── openspec/                      # OpenSpec仕様管理
│   ├── specs/                     # 承認済み仕様
│   ├── changes/                   # 変更提案
│   └── archive/                   # 完了した変更
└── ...
```

## セットアップ

### 1. 環境変数の設定

`.env`ファイルに必要な環境変数を設定します（`.env.example`を参照）：

```bash
cp .env.example .env
# 必要な環境変数を編集
```

### 2. Factory Droidの確認

```bash
# .factoryディレクトリが存在することを確認
ls -la .factory/

# 設定ファイルの確認
cat .factory/config.yml
```

### 3. 依存関係のインストール

```bash
# バックエンド
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# フロントエンド
cd frontend
npm install
```

## 利用可能なDroid

### 1. 開発ワークフロー自動化 (`dev-workflow`)

**用途**: テスト、ビルド、リントの自動実行

**実行タイミング**:
- コミット前
- PR作成時
- コード変更時

**チェック項目**:
- ✅ バックエンド: Ruff (lint/format)、mypy (型チェック)、pytest (テスト)
- ✅ フロントエンド: ESLint、TypeScript、テスト、ビルド
- ✅ セキュリティ: 依存関係監査、機密情報スキャン

**使い方**:
```bash
# 手動実行
/dev-check

# または、コード変更時に自動実行
```

### 2. マルチテナントガード (`multi-tenant-guard`)

**用途**: テナント分離違反の検出・防止

**重要性**: 🔴 **CRITICAL** - マージブロック対象

**チェック項目**:
- ❌ テナントフィルタなしのクエリ検出
- ❌ APIエンドポイントのテナント検証漏れ
- ❌ モデル定義の`tenant_id`カラム漏れ
- ❌ クロステナントアクセステスト不足

**使い方**:
```bash
# 手動実行
/tenant-check

# PR作成時に自動実行
```

**検出される問題例**:

```python
# ❌ BAD: テナントフィルタなし
assessments = db.query(Assessment).all()

# ✅ GOOD: テナントでフィルタリング
assessments = db.query(Assessment).filter(
    Assessment.tenant_id == current_tenant.id
).all()
```

### 3. OpenSpec統合 (`openspec-sync`)

**用途**: 仕様駆動開発のサポート

**チェック項目**:
- 📝 仕様ファイルの構造検証
- 🔄 仕様と実装の同期状態確認
- 📊 変更提案の追跡
- 📚 ドキュメントリンクの確認

**使い方**:
```bash
# 同期状態チェック
/spec-check

# 詳細分析
/spec-sync

# ワークフローガイド表示
/spec-help
```

## 基本的な使い方

### コマンド実行

Factory Droidは以下のコマンドで実行できます：

```bash
# 開発チェック
/dev-check

# テナント分離チェック
/tenant-check

# 仕様同期チェック
/spec-check
/spec-sync

# ヘルプ
/spec-help
```

### 自動実行

以下のタイミングで自動的に実行されます：

1. **PR作成時**: すべてのDroidが実行
2. **コード変更時**: 変更ファイルに応じてDroidが実行
3. **コミット前**: Git hooksで実行（設定による）

## 開発ワークフロー

### 1. 新機能開発

```bash
# Step 1: 仕様を確認
/spec-check
cat openspec/specs/{feature}/specification.md

# Step 2: ブランチ作成
git checkout -b feature/feature-name

# Step 3: 実装
# コードを書く...

# Step 4: 開発チェック
/dev-check

# Step 5: テナント分離チェック（重要！）
/tenant-check

# Step 6: コミット
git add .
git commit -m "feat: Add feature-name"

# Step 7: PR作成
git push origin feature/feature-name
gh pr create
# → 自動的にすべてのDroidが実行される
```

### 2. バグ修正

```bash
# Step 1: Issue確認
gh issue view {issue-number}

# Step 2: ブランチ作成
git checkout -b fix/bug-name

# Step 3: 修正実装

# Step 4: テスト追加（再発防止）
# tests/ に失敗ケースを追加

# Step 5: 開発チェック
/dev-check

# Step 6: コミット・PR
git commit -m "fix: Fix bug-name"
git push origin fix/bug-name
gh pr create
```

### 3. OpenSpec仕様駆動開発

```bash
# Step 1: 仕様提案
# openspec/changes/ に新しい仕様ファイルを作成
# または Claude Codeで: /openspec-proposal "feature description"

# Step 2: レビュー
# チームで仕様をレビュー・調整

# Step 3: 承認
# openspec/specs/ に移動（Source of Truth）

# Step 4: 実装
# 承認された仕様に基づいて実装
# Claude Codeで: /openspec-apply

# Step 5: 検証
/spec-check

# Step 6: アーカイブ
# 完了後、openspec/archive/ に移動
# Claude Codeで: /openspec-archive
```

## ベストプラクティス

### 1. マルチテナント開発

**必ず守るべきルール**:

✅ **すべてのクエリでテナントフィルタリング**
```python
# 良い例
items = db.query(Item).filter(
    Item.tenant_id == current_tenant.id
).all()
```

✅ **APIエンドポイントでテナント検証**
```python
@router.get("/api/v1/tenants/{tenant_id}/items")
async def list_items(
    tenant_id: UUID,
    current_user: User = Depends(get_current_user)
):
    # テナントIDの検証
    if current_user.tenant_id != tenant_id:
        raise HTTPException(status_code=403)
    ...
```

✅ **クロステナントアクセステスト**
```python
def test_cross_tenant_access_denied():
    """異なるテナントのデータにアクセスできないことを確認"""
    # テナントAのユーザーでログイン
    # テナントBのリソースにアクセス試行
    # → 403 Forbiddenを期待
```

### 2. コード品質

✅ **コミット前に必ず実行**
```bash
/dev-check
```

✅ **型アノテーションを追加**
```python
def get_user(user_id: UUID) -> User:
    ...
```

✅ **テストカバレッジ目標**
- バックエンド: 80%以上
- フロントエンド: 70%以上
- クリティカルパス: 100%

### 3. OpenSpec活用

✅ **実装前に仕様を確認**
```bash
cat openspec/specs/{feature}/specification.md
```

✅ **仕様と実装の同期を維持**
```bash
/spec-sync
```

✅ **変更提案を定期的にレビュー**
```bash
ls openspec/changes/
```

## トラブルシューティング

### dev-checkが失敗する

**症状**: `/dev-check`実行時にエラー

**解決方法**:

1. **Lintエラー**
   ```bash
   cd backend && ruff check . --fix
   cd frontend && npm run lint --fix
   ```

2. **Formatエラー**
   ```bash
   cd backend && ruff format .
   cd frontend && npm run format
   ```

3. **テスト失敗**
   ```bash
   cd backend && pytest tests/ -v
   cd frontend && npm test
   ```

4. **型エラー**
   ```bash
   cd backend && mypy app/
   cd frontend && npx tsc --noEmit
   ```

### tenant-checkが失敗する

**症状**: マルチテナント違反が検出される

**解決方法**:

1. **テナントフィルタを追加**
   ```python
   # 修正前
   items = db.query(Item).all()
   
   # 修正後
   items = db.query(Item).filter(
       Item.tenant_id == current_tenant.id
   ).all()
   ```

2. **APIエンドポイントにテナント検証を追加**
   ```python
   @router.get("/api/v1/tenants/{tenant_id}/items")
   async def list_items(
       tenant_id: UUID,
       current_user: User = Depends(get_current_user)
   ):
       if current_user.tenant_id != tenant_id:
           raise HTTPException(status_code=403)
       ...
   ```

3. **テストケースを追加**
   ```python
   def test_tenant_isolation():
       """テナント分離を検証"""
       # テストコードを追加
   ```

### spec-checkで警告が出る

**症状**: 仕様と実装の不一致が検出される

**解決方法**:

1. **仕様ファイルを確認**
   ```bash
   cat openspec/specs/{feature}/specification.md
   ```

2. **実装を仕様に合わせる**
   - 不足している機能を実装
   - 仕様と異なる部分を修正

3. **仕様を更新（必要に応じて）**
   - チームでレビュー
   - 承認後、openspec/specs/を更新

## コードテンプレート

`.factory/templates/`には以下のテンプレートが用意されています：

### バックエンド

- `api_endpoint.py`: FastAPI エンドポイント
- `model.py`: SQLAlchemyモデル
- `service.py`: ビジネスロジック
- `test_*.py`: pytestテストケース

### フロントエンド

- `component.tsx`: Reactコンポーネント
- `hook.ts`: カスタムフック
- `store.ts`: Zustandストア
- `*.test.tsx`: テストファイル

**使用例**:
```bash
# テンプレートからファイル生成
cp .factory/templates/api_endpoint.py backend/app/api/v1/new_feature.py
# 内容を編集...
```

## 参考リソース

### ドキュメント
- [プロジェクトコンテキスト](.factory/context.md)
- [CLAUDE.md](../CLAUDE.md) - Claude Code開発ガイド
- [README.md](../README.md) - プロジェクト概要
- [OpenSpec仕様](../openspec/specs/)

### 外部リンク
- [Factory AI](https://factory.ai/)
- [OpenSpec](https://openspec.dev/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [React Documentation](https://react.dev/)

### サポート
- **GitHub Issues**: バグ報告・機能要求
- **Repository**: https://github.com/yusuke-kurosawa/DiagnoLeads

---

**Happy Coding with Factory Droid! 🤖✨**
